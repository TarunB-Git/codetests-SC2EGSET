# Code Reuse

| Repository | File | Function/class | Use | Reuse mode | Correctness issue | New code |
| --- | --- | --- | --- | --- | --- | --- |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/transforms/utils.py` | `average_player_stats` | Produce the canonical per-player averaged PlayerStats features. | Wrapped unchanged. | Its output is keyed by player ID, so a label selected by list position is not necessarily aligned with row 0. | `src/latent_trainer/benchmarks/transforms/aligned_economy.py` sorts the keys and builds feature rows in that order. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/transforms/utils.py` | `select_outcome_1v1` | Produce binary outcomes keyed by player ID. | Wrapped unchanged. | No issue in this helper; the existing `economy_average_vs_outcome` does not use it and instead labels from `toonPlayerDescMap[0]`. | `src/latent_trainer/benchmarks/transforms/aligned_economy.py` indexes outcomes with the same sorted player IDs used for features. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/replay_parser/tracker_events/events/player_stats/stats.py` | `Stats` | Define the canonical averaged feature width. | Reused unchanged. | Duplicating the 39 field names or width would drift if the dataclass changes. | `src/latent_trainer/benchmarks/transforms/aligned_economy.py` derives the expected width with `dataclasses.fields`. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/torch/datasets/sc2_dataset_single_json.py` | `SC2DatasetSingleJSON.from_json_path` | Load and randomly index bracketed synthetic SingleJSON replays without reconstructing a loader. | Reused unchanged for bracketed SingleJSON only. | Its offset builder assumes an opening bracket and therefore skips record zero when given JSONL. | `src/latent_trainer/benchmarks/common/data.py` detects content and streams JSONL records through `SC2ReplayData.from_dict`, while bracketed SingleJSON continues to use the existing loader. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/features/preprocess_dataset.py` | `TransformEnumFunction`, `process_replay`, test-only and chunked preprocessing | Route the established preprocessing pipeline through the aligned per-player transform and preserve vector labels in caches. | Adapted. | The original scalar-label checks and `torch.tensor(labels)` construction did not support two aligned targets per replay. The existing ratio repair moved rejected-transform replacements from test into train or validation, invalidating held-out evaluation. Output-directory creation targeted the parent instead of the requested directory. | `process_replay` now validates scalar or vector labels, `stack_labels` preserves label dimensions, `averaged_economy` resolves to `economy_average_players_vs_outcomes`, each split retains only its own valid replays, and cache output paths are created directly. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/transforms/utils.py` | `filter_player_stats` | Select the existing per-player PlayerStats streams before sorting them by game loop for Tasks 1B, 2, and 3. | Wrapped unchanged. | The helper returns event objects rather than model-ready tensors, does not enforce temporal ordering, and can expose duplicate observations at one game loop. | `src/latent_trainer/benchmarks/common/data.py` orders the reused events, keeps one observation per player and game loop, rejects tracker-error replays, and converts the canonical `Stats` fields into variable-length tensors. |
| `Kaszanas/SC2EGSet_article_experiments` | `sc2_timeseries.ipynb` | `timepoint_fit` and the match filters | Preserve the historical `game_time_gameloop > 12000` filter and Logistic Regression, RBF SVM, and XGBoost settings for the historical protocol. | Adapted. | The notebook scales all data before cross-validation and uses ordinary shuffled `KFold`, which leaks fold information and can place rows from the same replay in different folds. | `src/latent_trainer/benchmarks/task1/static.py` keeps those choices only for labeled historical reproduction and provides a separate corrected protocol with train-only pipelines and replay-grouped cached splits. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/replay_data/sc2_replay_data.py` | `SC2ReplayData` fields and `ToonPlayerDesc.toon` | Preserve replay identifiers, timestamps, versions, maps, durations, persistent Toon identities, races, MMR, and aligned outcomes in benchmark samples. | Wrapped unchanged. | Positional order in `toonPlayerDescMap` is not a player-ID alignment guarantee. | `src/latent_trainer/benchmarks/common/data.py` indexes descriptions by explicit player ID and carries persistent identities only as split metadata, never as model inputs. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/features/rich_transform.py` | `rich_transform` | Retain the existing 196-feature representation for its original representation-learning workflows. | Reused unchanged outside Task 1A. | It includes early, middle, late, final, and delta summaries, so it exposes full-match outcome information and is not a valid averaged-data reproduction input. | `src/latent_trainer/benchmarks/task1/static.py` rejects rich caches and requires the aligned 39-feature `averaged_economy` cache. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/utils/json_utils.py` | `get_object_at_index` | Read one indexed replay object from JSONL or line-oriented SingleJSON byte offsets. | Reused unchanged behind a source abstraction. | The upstream offset builder skips the first JSONL record and writes offsets beside the raw file. | `src/latent_trainer/benchmarks/data/source.py` builds external, atomic, fingerprint-checked offsets by format and delegates individual object reads to `get_object_at_index`. |
| `Kaszanas/SC2_Datasets` | `src/sc2_datasets/replay_data/sc2_replay_data.py` | `SC2ReplayData.from_dict` | Construct the established replay types after indexed JSON retrieval. | Reused unchanged. | Parse failures need stable source-aware rejection records for a resumable corpus pass. | `src/latent_trainer/benchmarks/data/source.py` wraps failures with local and original source indices and stable rejection codes. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/models/guided_vae.py` | `suGuidedVAE`, `Classifier` | Train the Task 4 guided representation without introducing a second VAE. | Reused unchanged. | Existing training expected an in-memory cache without benchmark/cache compatibility metadata. | `src/latent_trainer/benchmarks/task4/` supplies shard-backed inputs, training-only streaming normalization, held-out probes and compatibility validation. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/models/lightning/lit_guided_vae.py` | `LitGuidedVAE` | Retain the existing Lightning optimization and adversarial guide training. | Adapted minimally. | Checkpoints did not identify the feature cache or split that produced them. | Checkpoints now carry cache, split, source, schema and input-dimension compatibility metadata. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/models/losses.py` | `loss_supervised` | Keep the established normalized-feature MSE reconstruction plus KL objective. | Reused unchanged. | Inputs must be normalized from training data only for the reconstruction scale to be meaningful. | `src/latent_trainer/benchmarks/task4/data.py` calculates and persists streaming training-only statistics. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/paths/strategies/linear.py` | `path_linear` | Construct the Task 4 latent path to a training winner centroid. | Reused unchanged. | Decoded points can leave feature ranges observed in training. | `src/latent_trainer/benchmarks/task4/counterfactual.py` validates unconstrained states and constrains decoded values to training-observed ranges. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/paths/feedback.py` | `compute_feedback` | Produce ranked feature-level differences along the counterfactual path. | Reused unchanged behind a JSON-safe adapter. | Its generic report does not encode provenance or distinguish association from causation. | `src/latent_trainer/benchmarks/task4/counterfactual.py` adds replay references, observed/derived separation, plausibility counts and an explicit non-causal label. |
| `Kaszanas/SC2_LatentTrainer` | `src/latent_trainer/benchmarks/common/models.py` | `GRUSequenceModel`, `TransformerSequenceModel`, `padded_batch` | Retain the corrected packed-GRU and padding-mask Transformer implementations for shard-backed sequence training. | Adapted in place. | The first implementation concatenated and normalized every sequence in memory before training. | Streaming moment calculation and lazy bounded batches now avoid a second full sequence copy, and cached Task 1B uses the saved split directly. |
